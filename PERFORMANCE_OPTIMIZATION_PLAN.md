# RASKRUTOV.KZ — ПЛАН ОПТИМИЗАЦИИ ПРОИЗВОДИТЕЛЬНОСТИ

## Назначение файла

Этот документ является последовательным техническим заданием для Cursor Agent по оптимизации главной страницы `raskrutov.kz`.

Работа начинается **с этапа 3**.

- Этап 1 «Технический аудит» — `COMPLETED`.
- Этап 2 «Удаление подтверждённого мусора» — `COMPLETED`, изменения уже применены.
- Этапы 1 и 2 не повторять, не откатывать и не переделывать без отдельной команды владельца проекта.
- Не выполнять `commit`, `push`, публикацию и любые действия с production до отдельной команды.

## Исходные показатели до оптимизации

| Режим | Performance | FCP | LCP | TBT | CLS | Speed Index |
|---|---:|---:|---:|---:|---:|---:|
| ПК | 66 | 0,6 с | 1,0 с | 400 мс | 0,246 | 1,6 с |
| Мобильный | 72 | 3,0 с | 4,7 с | 120 мс | 0,124 | 3,8 с |

Это исходная точка до выполнения всего комплекса работ. Если после уже применённых этапов 1–2 есть новые замеры, занести их в `PERFORMANCE_STATUS.md` как промежуточную точку, не заменяя исходные значения.

---

# УПРАВЛЯЮЩАЯ КОМАНДА ДЛЯ CURSOR AGENT

Скопировать весь текст следующего блока в новый чат Cursor в режиме Agent:

```text
Работаем над оптимизацией производительности сайта raskrutov.kz.

Главный документ задачи: PERFORMANCE_OPTIMIZATION_PLAN.md.

Сначала полностью изучи:
- AGENTS.md, если он существует;
- все правила в .cursor/rules;
- PERFORMANCE_OPTIMIZATION_PLAN.md;
- PERFORMANCE_STATUS.md, если он существует;
- структуру проекта;
- текущий git status и git diff;
- уже применённые изменения этапов 1 и 2.

ВАЖНО: этапы 1 и 2 уже выполнены и применены. Не повторяй их, не откатывай их изменения и не начинай новый общий аудит. Работа начинается строго с этапа 3.

Перед изменениями:
1. Покажи текущую ветку и git status.
2. Определи, есть ли незакоммиченные изменения после этапов 1–2.
3. Не удаляй и не перезаписывай эти изменения.
4. Создай или обнови PERFORMANCE_STATUS.md.
5. Отметь этапы 1 и 2 как COMPLETED.
6. Зафиксируй в статусе перечень уже изменённых файлов и доступные промежуточные метрики.
7. Покажи краткий план этапов 3–9 и затем приступай к этапу 3 без дополнительных вопросов, если нет реальной блокирующей проблемы.

Выполняй этапы 3–9 строго последовательно.

Правила выполнения:
1. Не объединяй этапы и не меняй их порядок.
2. Перед каждым этапом перечитывай его полный текст в PERFORMANCE_OPTIMIZATION_PLAN.md.
3. После каждого этапа обновляй PERFORMANCE_STATUS.md: статус, изменённые файлы, выполненные проверки, метрики, риски и остаточные задачи.
4. После каждого этапа обязательно проверь git diff, ошибки консоли, 404 по ресурсам, адаптивность и затронутые функции.
5. Не переходи дальше, если текущий этап вызвал визуальную поломку, ошибку JavaScript, 404 или функциональную регрессию. Сначала исправь её.
6. Не удаляй ресурс, пока не доказано отсутствие ссылок и зависимостей на всех страницах.
7. Не изменяй дизайн, тексты, URL, SEO, микроразметку, аналитику, robots.txt, sitemap.xml и логику отправки заявок.
8. Соблюдай правила форм из .cursor/rules. Не меняй endpoint, формат данных, name/data-атрибуты, сообщения, цели аналитики и логику отправки.
9. Не выполняй реальную отправку заявки без отдельного разрешения. Допустимы только безопасные локальные проверки и валидация обработчиков.
10. Не выполняй commit, push, merge, публикацию и изменение production.
11. Не устанавливай новые зависимости без доказанной необходимости. Если зависимость нужна, сначала объясни причину в отчёте.
12. Не используй массовую автоматическую очистку CSS/HTML/JS без ручной проверки динамических состояний.
13. Если внешний PageSpeed требует опубликованной версии, отметь проверку как POST-DEPLOY. Локально используй Lighthouse, если он доступен.
14. Если этап реально заблокирован, пометь его BLOCKED, подробно укажи причину и не имитируй успешное выполнение.
15. Указание «после отчёта остановись» внутри этапа означает завершить этот этап, обновить PERFORMANCE_STATUS.md и перейти к следующему этапу, если нет блокера.

Контрольные ширины:
- 360 px;
- 390 px;
- 768 px;
- 1024 px;
- 1440 px;
- 1920 px.

После этапа 9 подготовь единый итоговый отчёт:
- что исправлено;
- какие файлы изменены и удалены;
- показатели до и после;
- экономия CSS, JS, изображений и шрифтов;
- результаты проверки форм и интерактивных элементов;
- что нужно проверить после публикации;
- оставшиеся риски;
- готовность к commit и push.

После итогового отчёта остановись и жди отдельной команды. Ничего не публикуй.
```

---

# ШАБЛОН `PERFORMANCE_STATUS.md`

Cursor должен создать файл со следующей логикой:

```markdown
# PERFORMANCE STATUS — RASKRUTOV.KZ

## Текущая контрольная точка
- Ветка:
- Git status:
- Незакоммиченные изменения этапов 1–2:
- Дата локальной проверки:
- Доступность локального Lighthouse:

## Метрики
| Контрольная точка | Режим | Performance | FCP | LCP | TBT | CLS | Speed Index |
|---|---|---:|---:|---:|---:|---:|---:|
| До оптимизации | ПК | 66 | 0,6 с | 1,0 с | 400 мс | 0,246 | 1,6 с |
| До оптимизации | Мобильный | 72 | 3,0 с | 4,7 с | 120 мс | 0,124 | 3,8 с |
| После этапов 1–2 | ПК |  |  |  |  |  |  |
| После этапов 1–2 | Мобильный |  |  |  |  |  |  |

## Статусы этапов
- Этап 1 — COMPLETED ранее
- Этап 2 — COMPLETED ранее, изменения применены
- Этап 3 — PENDING
- Этап 4 — PENDING
- Этап 5 — PENDING
- Этап 6 — PENDING
- Этап 7 — PENDING
- Этап 8 — PENDING
- Этап 9 — PENDING

## Журнал этапов
### Этап N
- Статус:
- Изменённые файлы:
- Что сделано:
- Что проверено:
- Метрики:
- Риски/ограничения:
- POST-DEPLOY проверки:
```

---

# ЭТАП 3. LCP И CLS ПЕРВОГО ЭКРАНА

```text
Переходим к этапу 3: исправление LCP и CLS первого экрана raskrutov.kz.

Этапы 1 и 2 уже выполнены. Не повторяй их и не откатывай применённые изменения.

По исходному отчёту PageSpeed LCP-элемент находился по цепочке:
div#sections_list
> div#9466bf80aa894ca9b20b37b4d9409cc1
> div.section_image_container
> div.section-image

Изображение первого экрана загружалось через CSS background-image. На мобильной версии section_image_container создавал CLS около 0,123.

Перед изменениями проверь, сохранилась ли эта реализация после этапов 1–2. Работай с фактическим текущим кодом.

Нужно:
1. Найти точное изображение первого экрана и все его версии.
2. Сделать LCP-изображение обнаруживаемым браузером в начале загрузки.
3. Предпочтительно заменить CSS-фон на семантически нейтральный <picture>/<img>, абсолютно расположенный внутри блока, только если внешний вид останется пиксельно идентичным.
4. Задать корректные width/height или aspect-ratio, чтобы место резервировалось до загрузки ресурса.
5. Для LCP-изображения использовать loading="eager" и fetchpriority="high".
6. Настроить корректные srcset и sizes, если используются разные изображения или размеры по breakpoint.
7. Не применять loading="lazy" к LCP-изображению.
8. Если безопасная замена фона на <img> невозможна, добавить точный preload и заранее зарезервировать размеры контейнера через CSS.
9. Задать section_image_container устойчивые размеры для всех контрольных breakpoint до загрузки JS и изображения.
10. Удалить только те JS-перерасчёты высоты, которые доказанно вызывают позднее смещение и не нужны для адаптива.
11. Не создавать дублирующую загрузку одного LCP-изображения одновременно через CSS и HTML.
12. Проверить, что первый экран не появляется рывком и не меняет высоту после загрузки.

Не менять:
- композицию первого экрана;
- тексты, кнопки и расположение объектов;
- затемнение, цветокоррекцию и кадрирование;
- ПК- и мобильные пропорции;
- SEO, микроразметку, формы и аналитику.

Цели:
- мобильный LCP менее 2,5 с;
- CLS менее 0,1;
- отсутствие визуального отличия до/после.

Проверка:
- ширины 360, 390, 768, 1024, 1440 и 1920 px;
- отсутствие двойной загрузки LCP в Network;
- отсутствие 404;
- отсутствие новых ошибок консоли;
- локальный Lighthouse до/после, если доступен.

В конце:
1. Обнови PERFORMANCE_STATUS.md.
2. Укажи изменённые файлы.
3. Опиши старый и новый способ загрузки LCP.
4. Запиши результаты визуальной и технической проверки.
5. Отдельно перечисли проверки POST-DEPLOY.

Не выполнять commit, push и публикацию.
```

---

# ЭТАП 4. ОПТИМИЗАЦИЯ ИЗОБРАЖЕНИЙ

```text
Переходим к этапу 4: оптимизация изображений raskrutov.kz.

Используй текущее состояние после этапа 3. Не оптимизируй файлы вслепую. Сначала сопоставь фактический размер каждого изображения с размером его контейнера на разных экранах.

В исходном отчёте PageSpeed были обнаружены изображения примерно вдвое больше контейнеров, включая:
- 696×492 при контейнере 348×246;
- 878×492 при контейнере 439×246;
- 1054×490 при контейнере 527×245;
- 294×632 при контейнере примерно 147–170×321–372;
- иконки около 183×186 при контейнере около 61×62.

Сначала найди фактические соответствующие файлы в текущем проекте. Не считай размеры из старого отчёта доказательством, если код уже изменился.

Нужно:
1. Составить таблицу: файл, исходный размер в пикселях, вес, фактический размер отображения, страницы использования.
2. Для растровых контентных изображений создать оптимальные WebP/AVIF-варианты под реальные размеры.
3. Настроить srcset и sizes там, где размер заметно меняется между breakpoint.
4. Оставить разумный запас для экранов высокой плотности, не отдавая всем устройствам максимальный файл.
5. Исправить фиктивные width="10" height="10" и указать реальные пропорции.
6. Для изображений ниже первого экрана использовать loading="lazy" и decoding="async".
7. Для LCP сохранить loading="eager" и fetchpriority="high".
8. Не назначать fetchpriority="high" второстепенным изображениям первого экрана.
9. Сохранить визуальное качество, кадрирование, object-fit, border-radius и композицию.
10. Не менять содержательные alt. Пустые или некорректные alt исправлять только после определения назначения изображения.
11. Не удалять оригинал, если он используется на другой странице, в поп-апе, галерее или большем размере.
12. Не конвертировать SVG в растр без технической необходимости.
13. Не допускать увеличения количества запросов без выигрыша по передаваемому объёму.

После изменений проверь:
- отсутствие битых ссылок и 404;
- отсутствие растянутых или размытых изображений;
- галереи, слайдеры и поп-апы;
- ПК и мобильную версию;
- отсутствие нового CLS;
- правильный выбор srcset в браузере.

В отчёте укажи:
- старый и новый вес каждого изменённого файла;
- общую экономию;
- какие оригиналы сохранены и почему;
- результаты проверок.

Обнови PERFORMANCE_STATUS.md. Не выполнять commit, push и публикацию.
```

---

# ЭТАП 5. ШРИФТЫ И CLS

```text
Переходим к этапу 5: оптимизация шрифтов raskrutov.kz.

По исходному отчёту в критической цепочке загружались:
- Open Sans WOFF около 66 КБ;
- Montserrat normal/medium WOFF примерно по 106 КБ;
- Inter normal/bold WOFF примерно 132–141 КБ.

Шрифты могли вызывать смещение блока m-wrapper--with-slider. Исходный CLS на ПК достигал 0,246.

Сначала проверь фактические шрифты и начертания в текущем коде после этапов 1–4.

Нужно:
1. Определить реально используемые семейства, начертания и веса на главной странице и в общих компонентах.
2. Найти повторные @font-face и дублирующие подключения.
3. Удалить подключения неиспользуемых семейств и весов только после проверки всех страниц.
4. Конвертировать нужные WOFF в WOFF2, если эквивалентных WOFF2 ещё нет.
5. Обязательно сохранить кириллицу и используемые казахские символы. Не создавать Latin-only subset.
6. Не менять визуально семейство, толщину, размеры и интервалы текста.
7. Предзагружать только 1–2 действительно критичных файла первого экрана. Не preload всех шрифтов.
8. Настроить font-display с учётом минимизации блокировки и скачков текста.
9. Создать метрически совместимые fallback-настройки через size-adjust, ascent-override, descent-override и line-gap-override, где это поддерживается и оправдано.
10. Устранить изменение высоты строк и переносов заголовков после загрузки шрифта.
11. Удалить старые WOFF только после проверки отсутствия ссылок во всех HTML/CSS/JS.
12. Не подключать внешние Google Fonts, если шрифты уже локальные.

Проверь:
- первый экран;
- блок «Ключевые направления и продукты»;
- меню и мобильное меню;
- кнопки;
- формы и поп-апы;
- переносы заголовков и текстов;
- все контрольные ширины;
- CLS и отсутствие заметного скачка текста.

Цель:
- CLS менее 0,1;
- отсутствие заметного FOIT/FOUT;
- сохранение исходного дизайна.

В конце покажи:
- использованные семейства и веса;
- удалённые и созданные файлы;
- старый и новый общий вес шрифтов;
- результаты визуальной проверки и Lighthouse.

Обнови PERFORMANCE_STATUS.md. Не выполнять commit, push и публикацию.
```

---

# ЭТАП 6. УДАЛЕНИЕ НЕИСПОЛЬЗУЕМОГО CSS

```text
Переходим к этапу 6: переработка CSS raskrutov.kz.

Исходный PageSpeed показывал:
- pages/public.bundle*.css: около 67 КБ, почти весь код не использовался;
- home-deferred.v3.css: около 42 КБ, не использовалось 15–22 КБ;
- home-popup-2782231.v2.css и home-critical.v3.css блокировали отрисовку;
- потенциальная экономия неиспользуемого CSS: 80–87 КБ.

Этап 2 уже удалил подтверждённый мусор. Не повторяй его. Работай с оставшимися зависимостями в текущем состоянии проекта.

Задача: убрать остаточную зависимость главной страницы от общего CSS конструктора, если она ещё существует, сохранив внешний вид и функциональность.

Порядок:
1. Проверь, какие CSS-файлы фактически подключены сейчас и какие правила из старого bundle всё ещё используются.
2. Собери минимальный набор действительно нужных правил.
3. Обязательно учти динамические состояния:
   - открытое мобильное меню;
   - открытые поп-апы;
   - активные слайды;
   - ошибки, загрузку и успешное состояние форм;
   - hover/focus/active;
   - социальный виджет;
   - скрытые и раскрытые элементы;
   - состояния, добавляемые JavaScript во время работы.
4. Перенеси необходимые правила в локальные CSS-файлы проекта.
5. Только после визуального и функционального сравнения отключи оставшийся public.bundle*.css от главной страницы.
6. Выдели небольшой critical CSS для шапки и первого экрана.
7. Некритические стили загружай отложенно только безопасным способом с noscript fallback.
8. Стили поп-апов можно отложить, но при первом открытии не должно быть FOUC.
9. Удали дублирующиеся, переопределённые и недостижимые правила.
10. Замени transition: all конкретными свойствами.
11. Сохрани исходники читаемыми. Минификацию выполняй только в рамках существующего процесса сборки или отдельного production-файла.
12. Не создавать хрупкую загрузку CSS через JavaScript, если обычный defer-паттерн решает задачу безопаснее.

Запрещено:
- автоматически запускать PurgeCSS без полного safelist динамических классов;
- массово переименовывать HTML-классы;
- менять дизайн или breakpoints без необходимости;
- удалять стили форм и состояний ошибок;
- удалять стили скрытых поп-апов только потому, что они не видны при первом рендере;
- переносить весь CSS в inline.

Проверь все основные блоки и интерактивные состояния на ширинах 360–1920 px.

В отчёте покажи:
- какие правила извлечены из старого bundle;
- отключён ли старый CSS полностью;
- старый и новый объём CSS;
- какие файлы блокируют первый рендер после изменений;
- результаты визуальной и функциональной проверки.

Обнови PERFORMANCE_STATUS.md. Не выполнять commit, push и публикацию.
```

---

# ЭТАП 7. ОПТИМИЗАЦИЯ JAVASCRIPT

```text
Переходим к этапу 7: оптимизация JavaScript raskrutov.kz.

Исходный PageSpeed показывал:
- pages/public.bundle*.js: около 151 КБ;
- неиспользуемый код: 106–111 КБ;
- public.bundle вызывал принудительную компоновку и длительные задачи;
- lead-forms.js создавал отдельную длительную задачу около 118 мс.

Этап 2 уже удалил подтверждённый мусор. Не повторяй его. Сначала проверь фактические оставшиеся JS-файлы и зависимости.

Нужно:
1. Составить список реально используемых функций оставшегося public.bundle, если он ещё подключён.
2. Заменить общий bundle небольшими локальными модулями только для реально используемых функций:
   - меню;
   - поп-апы;
   - слайдеры;
   - социальный виджет;
   - ленивое подключение YouTube;
   - остальные фактически используемые элементы.
3. Подключать скрипты через defer или type="module", учитывая совместимость и порядок инициализации.
4. Инициализировать функцию только при наличии соответствующего элемента в DOM.
5. Исключить повторные обработчики событий и двойную инициализацию.
6. Использовать делегирование событий там, где это безопасно.
7. Разделить чтение и запись layout-свойств.
8. Не читать offsetWidth/getBoundingClientRect сразу после изменения стилей.
9. Пакетировать визуальные изменения через requestAnimationFrame, когда это реально сокращает layout thrashing.
10. Удалить ненужные полифилы старых браузеров, только если проект не заявляет их поддержку.
11. После полной замены функций отключить public.bundle*.js от главной страницы.
12. Не создавать один новый монолитный файл взамен старого bundle.

Особые ограничения для lead-forms.js:
- соблюдать правила из .cursor/rules;
- не менять endpoint;
- не менять формат и названия полей данных;
- не менять name/data-атрибуты форм;
- не менять сообщения об успехе и ошибке;
- не менять маску телефона;
- не создавать второй submit-обработчик;
- не логировать персональные данные;
- не выполнять реальную отправку без разрешения;
- оптимизировать инициализацию только при полном сохранении поведения.

Сохранить Яндекс Метрику и действующие события аналитики.

Проверить:
- каждую форму без реальной отправки;
- меню и мобильное меню;
- все поп-апы;
- все слайдеры;
- YouTube;
- социальный виджет;
- телефон, WhatsApp и CTA;
- клавиатурное управление и Escape для диалогов, если оно уже предусмотрено;
- отсутствие ошибок и предупреждений в консоли;
- отсутствие 404;
- отсутствие повторных обработчиков.

В конце покажи:
- заменённые функции;
- отключён ли старый bundle полностью;
- старый и новый объём JS;
- результаты проверки;
- изменения TBT и main-thread work по локальному Lighthouse.

Обнови PERFORMANCE_STATUS.md. Не выполнять commit, push и публикацию.
```

---

# ЭТАП 8. DOM И АНИМАЦИИ

```text
Переходим к этапу 8: сокращение DOM и исправление анимаций raskrutov.kz.

Исходный PageSpeed обнаружил:
- 4 236 DOM-элементов;
- глубину DOM 44 уровня;
- большое количество вложенных SVG;
- 55–66 некомбинированных анимаций;
- анимации padding, border, color, visibility и других свойств.

Работай с текущим DOM после этапов 3–7. Не используй старое количество элементов как текущее значение — измерь заново.

Нужно:
1. Зафиксировать текущее количество DOM-элементов и максимальную глубину до изменений.
2. Найти пустые, служебные и лишние wrapper-элементы.
3. Удалять только обёртки, которые не влияют на CSS, сетку, JS, доступность и микроразметку.
4. Найти дубли ПК/мобильной версии. Если содержание одинаковое, по возможности заменить единым адаптивным блоком.
5. Не удалять уникальные SEO-тексты и полезный контент.
6. Повторяющиеся тяжёлые inline SVG заменить внешними SVG-файлами или SVG-спрайтом, сохранив внешний вид и доступность.
7. Сократить глубину вложенности карточек и декоративных элементов.
8. Не использовать display:none как имитацию сокращения DOM, если тяжёлый дубликат остаётся в HTML.
9. Заменить анимации width, height, padding, margin, top, left и border-width на transform/opacity, где это визуально эквивалентно.
10. Удалить transition: all.
11. Для изменения цвета оставить короткий transition конкретного свойства, если он не вызывает layout.
12. Добавить корректное поведение prefers-reduced-motion.
13. Не отключать все анимации глобально.
14. Не ухудшать семантику ради уменьшения числа узлов.

Ориентир: заметно сократить DOM, желательно ниже 2 500–3 000 элементов, но не ценой дизайна, контента, доступности или функциональности. Если безопасно достичь этого нельзя, укажи фактический результат и причины.

Проверить:
- первый экран;
- блок направлений;
- карточки;
- слайдеры;
- формы;
- поп-апы;
- футер;
- hover/focus/active;
- prefers-reduced-motion;
- все контрольные ширины;
- микроразметку и семантические элементы.

В отчёте укажи:
- количество DOM-элементов и глубину до/после;
- удалённые типы обёрток;
- оптимизированные SVG;
- изменённые анимации;
- результаты визуальной и функциональной проверки.

Обнови PERFORMANCE_STATUS.md. Не выполнять commit, push и публикацию.
```

---

# ЭТАП 9. КЭШИРОВАНИЕ И ФИНАЛЬНАЯ ПРОВЕРКА

```text
Переходим к этапу 9: кэширование и финальная проверка raskrutov.kz.

Сначала изучи текущую серверную конфигурацию проекта: .htaccess, правила PS.kz/Plesk, имена ресурсов и существующее кэширование. Не добавляй несовместимые директивы вслепую.

Нужно:
1. Настроить сжатие текстовых ресурсов через Brotli или gzip с безопасными IfModule-условиями, если это управляется проектом и поддерживается сервером.
2. Не дублировать уже существующее серверное сжатие конфликтующими правилами.
3. Настроить browser cache:
   - версионированные CSS/JS и неизменяемые изображения — длительный срок;
   - WOFF2 — длительный срок;
   - HTML — без агрессивного кэширования;
   - robots.txt и sitemap.xml — без годового immutable-кэша.
4. Для CSS/JS без хэша обеспечить cache busting через версию или новое имя файла.
5. Не назначать immutable ресурсу, имя которого не меняется при обновлении.
6. Проверить MIME-типы WebP, AVIF, SVG и WOFF2.
7. Проверить отсутствие 404, смешанного контента и дублирующихся загрузок.
8. Проверить консоль браузера.
9. Проверить формы без реальной отправки: валидацию, единственный submit-обработчик, формирование payload и обработку результата.
10. Проверить меню, поп-апы, слайдеры, YouTube, телефон, WhatsApp и социальные кнопки.
11. Проверить SEO:
   - Title и Description;
   - H1;
   - canonical;
   - микроразметку;
   - robots.txt;
   - sitemap.xml.
12. Проверить доступность клавиатурой и focus-состояния.
13. Выполнить три локальных Lighthouse-прогона для ПК и мобильного режима, если среда это позволяет, и использовать медианные значения.
14. Сравнить результат с исходной и промежуточными контрольными точками.

Целевые показатели:
- Performance не ниже 90, если достижимо без нарушения функциональности;
- мобильный LCP менее 2,5 с;
- CLS менее 0,1;
- TBT менее 200 мс;
- отсутствие ухудшения SEO, доступности и Best Practices.

Если локальная среда не позволяет достоверно проверить серверное сжатие, кэш и внешний PageSpeed, пометь эти пункты как POST-DEPLOY. Не утверждай, что они проверены.

Подготовь итоговый отчёт:
1. Метрики до и после.
2. Изменённые файлы.
3. Удалённые зависимости конструктора.
4. Экономия CSS, JS, изображений и шрифтов.
5. Результаты безопасной проверки каждой формы.
6. Результаты проверки интерактивных элементов.
7. Оставшиеся ограничения и риски.
8. Список POST-DEPLOY проверок.
9. Список файлов, которые должны быть опубликованы.
10. Готовность к commit и push: READY или NOT READY с причиной.

Обнови PERFORMANCE_STATUS.md.

Не выполнять commit, push и публикацию. После итогового отчёта остановись и жди отдельной команды владельца проекта.
```

---

# КОНТРОЛЬНЫЕ ТОЧКИ ВЛАДЕЛЬЦА ПРОЕКТА

После автоматического выполнения этапов 3–9 не публиковать изменения сразу.

Перед отдельной командой на commit/push проверить:

1. Первый экран на ПК и мобильном устройстве.
2. Мобильное меню.
3. Все поп-апы.
4. Слайдеры и галереи.
5. Все формы без отправки, затем одну реальную тестовую отправку только по отдельному разрешению.
6. Телефон, WhatsApp и CTA-кнопки.
7. Отсутствие визуальных отличий в текстах, сетках, отступах и шрифтах.
8. `PERFORMANCE_STATUS.md` и итоговый отчёт Cursor.
9. `git diff` и перечень удалённых файлов.
10. После публикации — PageSpeed Insights для ПК и мобильного режима, консоль, Network, серверный cache-control, gzip/Brotli и реальная заявка.

